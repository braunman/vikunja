import { expect, test as setup } from '../src/fixtures/web_fixture';
import { User } from '../src/helper/ua.object.builder';
import {saveToFile} from "../src/helper";



setup('Create new user', async ({ webApp }) => {
    // const user = new User()
    // await webApp.registerPage.open()
    // await webApp.registerPage.registerUser(user)
    // await expect(webApp.mainPage.loginUsername).toContainText(user.username)
    // await webApp.page.context().storageState({path: '.auth/login.json'})
    // await saveToFile(user,'.auth/user.json')
});